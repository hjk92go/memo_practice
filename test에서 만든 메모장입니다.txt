
<git>
깃을 통해 현재 컴퓨터에 저장
<github>
웹에 저장 ------------------------------git add remote origin 연결레파지토리 주소 (처음 한번만)
git push origin master(master라는 위치를 의미, 이름은 다를수있음) (필요에따라 작성)


git bash here 을 사용 하면 현재 폴더가 열림 

git init - 초기화 (처음 한번만)
git add . - 폴더안의 모든 내용 스냅샷(스크린샷처럼 기록) - 여러번해도 로그로 남지 않는다(필요에따라 작성)
git commit -m "" - 내용 저장
-------------------------------------------------------------여기까지 컴퓨터에 저장됨

새로운 내용을 추가적으로 작성하고 싶을때 브랜치를 새로만들어 사용한다
git branch 원하는브랜치 이름

git checkout 가고싶은브랜치 이름

---------------------------------------------------------------------------------
나중에 여러 브랜치를 마스터 브랜치로 병합하기 >> merge
git merge 병합하고싶은 브랜치이름

---------------------------------------------------------------------------
git branch -d 삭제하고싶은 브랜치 이름




